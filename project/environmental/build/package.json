{
  "name": "environmental",
  "version": "1.0.0",
  "description": "SGA - Environmental Module",
  "main": "index.ts",
  "scripts": {
    "build": "rm -rf ./build && npx tsc",
    "start": "npm run build && cp package*.json build/environmental/ && node build/environmental/index.js",
    "start:dev": "npx nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "enviromental",
    "processes"
  ],
  "author": "Isaac Marinho",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^4.12.0",
    "express": "^4.18.2",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.11.9",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  }
}
